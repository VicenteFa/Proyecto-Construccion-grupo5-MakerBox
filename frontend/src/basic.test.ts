import { describe, it, expect } from 'vitest';

describe('Configuracion inicial del Frontend', () => {
  it('prueba basica del frontend', () => {
    const suma = 1 + 1;
    expect(suma).toBe(2);
  });
});
